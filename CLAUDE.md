# Forge — Project Context

## What this is
A personal fitness tracking PWA. Mobile-first, vanilla JS frontend, Node + Express + Prisma + Postgres backend, deployed on Railway. Auto-deploys from main branch on GitHub.

## Who uses it
- Jay Shakeel (founder) — primary user, in active fat loss cut from 115kg to 90kg @ 15% BF
- Family members (Phase 42) — male/female, incl. under-18s; personalised onboarding wizard at signup
- Jay's friend (planned) — onboarding TBD

## Stack
- Frontend: vanilla JS PWA in /public/ (no build step for client)
- Backend: TypeScript + Express + Prisma in /server/
- DB: Postgres on Railway
- Auth: JWT (30-day expiry), bcrypt password hashing
- Push notifications: web-push + VAPID
- Cron: node-cron in same Express service
- External APIs: Oura (PAT), Withings (OAuth)
- Coaching: Cowork (Anthropic Claude) connected via Personal Access Tokens

## Domains/services connected
- Forge: forge-pwa-production.up.railway.app
- DB: Railway Postgres
- Oura: read-only via PAT
- Withings: OAuth (read-only metrics scope)
- Cowork: Jay's coaching agent connected via SHA-256-hashed PAT

## Active plan (May 2026)
- Daily calorie targets: 2,500 (gym day) / 2,400 (rest day)
- Macros: 250g protein / 210g carbs / 75g fat
- Eating window: 12:00-18:00 UK
- Training: alternating Upper/Rest/Lower/Rest, 4-day cycle, anchored 2026-05-08, 4pm workout time
- Mounjaro weekly Wednesday 18:00 (dose self-managed in Forge → More → Medications; CLAUDE.md no longer hardcodes)
- Meds: Metformin 1000mg with Meal 1, Statin with Meal 3, Vit D + Omega 3 daily, Creatine 5g in shake

## Coaching cadence
- Sunday 09:00 UK: scheduled task in Cowork pulls user data via /api/export, generates report, pushes to Forge via /api/coaching-reports
- Macros + meal plan auto-adjust based on weight/BF/TDEE trends
- Cowork CAN push: meal plans, profile fields (calsGym/calsRest/macros), reminders, coaching reports

## Phases shipped
- 1: Backend skeleton + auth + Railway deploy
- 1.5: UX polish (autofill fix, confirm pwd, max-width, secure logout)
- 2: Data sync to Postgres (cross-device)
- 2.1: Mobile safe-area fix
- 2.5: Train page week strip + history navigation + previous-session reference
- 3: Cowork connection + body fat tracking + remove AI Coach UI
- 4: Meal plan system (Today's Plan on Food page)
- 5: PWA push notifications + meal-time reminders
- 6: Oura auto-sync (sleep, steps, recovery, calories, workouts)
- 7: Withings auto-sync (weight, body fat, muscle mass, visceral fat)
- 8: AI coaching reports endpoint + Coach page display
- 8.1: Bug fixes (baseline week, water reset, planStartDate)
- 8.2: Profile auto-update endpoint (Cowork manages macros)
- 8.3: Generic reminders system (day-of-week + bi-weekly)
- 8.4: Alternating training schedule (Upper/Rest/Lower/Rest)
- 9: Dashboard rebuild (action-first Today page + sparklines)
- 11: Focused workout mode + smart progression + RIR effort
- 12: Critical security (token hashing, rate limit, helmet, health check, graceful shutdown) + remove photos + this CLAUDE.md
- 14: Daily evening + weekly preview cron nudges (protein check 22:00, weekly summary Sun 21:00)
- 16a: Field-scoped atomic state endpoints (jsonb_set) to fix concurrent write races
- 16b: UTC date bug fix — all date helpers now use Europe/London timezone
- 16c: Source tagging (manual/withings/oura) — manual entries protected from sync overwrite
- 16d: Network-first service worker for HTML/JS — deploys reflect on next page load
- 16e: Fix friend onboarding — set planStartDate + trainingStartDate on signup + migrate existing users
- 18: Granular meal logging — per-ingredient checkboxes, supplement tracking (supplementLog), expandable grouped food log, structured meal plan format (ingredients/supplements arrays)
- 18a: Fix race condition in logMealFromModal — single atomic PUT instead of N racing PUTs (clear + one per ingredient)
- 19: Standalone supplements tracker — state.supplements[] CRUD, Today page checklist, Coach page 7-day heatmap + 30-day adherence, More page management, meal modal integration via mealId, 21:00 missed-supplements cron push
- 20: Time-based exercise tracking — isometric holds (plank, side plank, dead hang, wall sit, hollow hold, l-sit) track {seconds, done} instead of {weight, reps}, live count-up timer in guided workout mode, "Could you have held longer?" effort prompt (easy/hard/maybe), seconds-progression (+5s on easy/maybe), PB = longest single hold, one-shot migration of historical data, isTimeBased() helper with keyword fallback
- 21: Oura sleep sync fix — three changes: (a) relaxed type filter so late_nap/rest entries 5+ hours count as main sleep, fixes daytime-shifted patterns where Oura mis-classifies long sleeps; (b) extended query end_date by 1 day to handle Oura's day-attribution boundary for daytime sleep; (c) removed aggressive auto-delete of stale entries — Oura updates if new data, otherwise leaves existing data alone
- 22: Progress dashboard on Track page — 4 metric cards (weight, BF, lean mass, visceral fat) with progress bars, sparklines, 14-day avg rates, projected goal date, LBM drop alert, /api/export includes computed progress
- 22a: Fix progress profile baseline for Jay — progressMigrationApplied guard, server-side migration on startup
- 22b: Linear regression goal projections — calcRate() using least-squares regression on all entries since start, getProjections() projects weight + BF goal dates separately with binding constraint, 5th GOAL DATE card with date range + confidence level (high 14+d / medium 7-13d / low <7d) + elapsed progress bar, export endpoint includes full projections for Cowork
- 23: BYOK AI Coach — replaces Cowork-driven weekly review with server-side generation using each user's own Anthropic API key (Opus 4.7). Key stored encrypted at rest (AES-256-GCM, `ENCRYPTION_KEY` env var). New routes `/api/coach/{key,test,generate-now}` (JWT). New `/api/coaching-reports/:rid/{apply,dismiss}/:sid` for user-driven application of structured suggestions (macros/reminders/note types). Sunday 09:00 UK cron generates per-user, 24h cost guard. Coach page renders Apply/Dismiss buttons under each report. `formatCoachingReport` now escapes HTML before markdown (closes XSS surface from LLM output). Legacy POST /api/coaching-reports kept for backward compat.
- 24: Food preferences profile — `state.profile.foodPrefs = { excluded[], notes, refreshCadence }`, atomic write via `PUT /api/state/profile/food-prefs` (jsonb_set, JWT). New "Food Preferences" section on More page with excluded-chips, free-text notes (2000 char cap), and plan-refresh cadence dropdown (weekly-sunday / biweekly / manual). Foundation for Phase 26 plan generation.
- 25: Portion control + log timestamps — meal detail modal replaces checkbox model with 5-button portion stepper (✕ · ½ · 1× · 1½ · 2×). Macros recompute live as portions change. Each food entry stores `quantity` (default 1.0) and `loggedAt` (ISO). Day detail + food log show actual-eaten time from loggedAt (fallback to planned time) and a `(½)` orange badge when quantity ≠ 1. Backward-compatible with old entries.
- 26: AI Coach generates the meal plan — `generateMealPlan()` uses Opus 4.7 with tool use to produce a structured plan, validates each ingredient name against `foodPrefs.excluded` server-side (rejects on match). Uses the user's actual logged-foods frequency (last 14d) so the plan reuses what they already eat. New routes `/api/coach/regenerate-plan` (JWT, 1h rate limit). New `lastMealPlanRegenAt` state field. Full-regen is now an "advanced" path; default is items-locked via Phase 26a.
- 26a: Items-locked plan editing — items in your plan never change automatically. New `recomputeMealPlanMacros()` (Opus 4.7, single batched call across all ingredients) keeps item names identical and only fills accurate per-ingredient macros from canonical reference data. Per-ingredient `edited: true` flag persists user overrides through auto-refreshes. New routes: `POST /api/coach/recompute-macros` (JWT, 1-min rate limit), `PUT /api/state/meal-plan` (JWT, validates against `foodPrefs.excluded`). Sunday cron switched from full regen to macros recompute (items locked). Meal detail modal: tap ingredient name to open edit modal (name + per-macro fields + delete); "+ Add ingredient" button appends new items; meal totals recompute from ingredient sums on every save. Food page button now reads "↻ Compute exact macros (keep items)".
- 26b: Seeded Jay's locked meal plan — startup migration `seedJayMealPlan()` writes Jay's exact 4-meal plan (breakfast eggs+oats+yogurt+berries, pre-workout chicken+basmati+veg, post-workout shake, dinner chicken+tomato stew + side salad). Guarded by `state.mealPlanSeededV2` so it only runs once.
- 27: Coach accuracy upgrade — three new things feed into the AI Coach report. (a) `state.profile.personal = { age, heightCm, sex, ethnicity, activityLevel }` via new `PUT /api/state/profile/personal` (JWT, jsonb_set). (b) `state.profile.medications = [{id, name, dose, schedule, notes}]` via new `PUT /api/state/profile/medications`. (c) Existing Oura recovery (`state.recovery[date] = { readiness, hrv, restingHR }`) now passed to coach context. AI prompt extended with med-aware reasoning rules (GLP-1 / statin / metformin / insulin specifics), Mifflin-St Jeor TDEE calculation (BMR × activity factor), and Oura recovery interpretation guidance (falling HRV + rising RHR = deload signal). "Personal Profile" and "Medications" sections added to More page. Sex required to compute BMR; flagged in context if missing.
- 28: Training progression v2 — three upgrades to `suggestWeight`/`suggestTime` in workout.js. (a) Recovery gate: reads today's `state.recovery[today]` Oura readiness/HRV plus 4-day HRV trend. Readiness < 60 or HRV down 3+ days running → every suggestion overrides to "Hold weight today — recover, don't push" with yellow banner on workout outline. (b) Stall detection: looks at last 5 sessions of same type via new `getPreviousSessions()` helper. Same weight held for 3+ sessions without hitting top of rep range → prescribes deload (drop weight by ~12%, rounded to 0.25kg, with reason "stalled N sessions — try Xkg today, back to Ykg next time"). (c) Per-lift increment scales: each WORKOUTS exercise tagged `size: small | medium | large`. Large compounds (leg press, RDL, hip thrust): +5/+2.5/-5kg. Medium (chest press, shoulder press, row, lat pulldown, leg ext/curl, calf raise): +5/+2.5/-2.5 (unchanged). Small isolation (curl, tricep pushdown, face pull, back ext, ab crunch): +2.5/+1.25/-1.25. DELOAD and HOLD badges shown in outline view next to affected lifts.
- 29: Coach intelligence v2 — six new data streams into the AI Coach context + sharper system prompt. (a) BODY COMPOSITION: current LBM (computed from latest weight × BF), muscle mass + visceral fat + hydration from Withings `bodyComp`, with 7d/14d deltas of weight / LBM / fat mass. New `latestBodyComp()` and `bodyCompAtDate()` helpers in ai-coach.ts. (b) SLEEP STAGES: Oura sync now extracts `rem_sleep_duration`, `deep_sleep_duration`, `light_sleep_duration`, `awake_time` per night and stores `remMin/deepMin/lightMin/awakeMin` on each `sleepLog[date]` entry. Coach prompt instructed to weight stages over total hours. (c) OURA ACTIVITY: daily steps + active calories from `stepsLog` / `calorieLog`. 7d avg active cal flagged as primary TDEE input over Mifflin estimate. (d) MEAL-PLAN ADHERENCE: per-day % of planned meals + ingredients actually logged (computed against `state.mealPlan` + `state.foods[date]`). (e) EFFORT DISTRIBUTION: % of sets tagged easy/solid/tough across last 7 days, with sandbag/under-recovery flag. (f) COACH MEMORY: last 4 `coachingReports` (with their `suggestions[]` apply/dismiss status) injected so the coach references and builds on prior advice instead of repeating. System prompt rewritten with goal-specific LBM-preservation framing (`Target LBM ≈ current LBM. Watch LBM week-over-week. Drop > 0.3kg/wk for 2+ weeks = urgent flag.`), explicit medication reasoning rules, sleep stage interpretation, and a hard rule never to repeat dismissed suggestions without new justification. Frontend: new Body Composition Trend card at top of Track page showing weight/fat-mass/LBM with 7d + 14d deltas, color-coded (green LBM stable, amber small drop, red losing muscle). Also: `fixJayVisceralTarget()` startup migration resets Jay's `profile.targetVisceralFat` from 10 → 6 (his current ~6.3 was already healthy; old target encouraged regression).
- 29a: Blood markers — clinical results flow into the AI Coach as ground-truth context. `state.profile.bloodMarkers[]` = `[{id, name, value, unit, refLow, refHigh, date, category, notes}]`. New atomic `PUT /api/state/profile/blood-markers` (JWT, jsonb_set, 100-marker cap). New "Blood Markers" section on More page — grouped by category with color-coded status (green in-range, amber out-of-range), tap to edit, latest panel date in header. New `modal-blm-edit` with name + value + unit + ref range + date + category + notes. Coach context now includes a BLOOD MARKERS block listing OUT OF RANGE markers prominently (with refs + per-marker notes) plus a brief in-range summary. System prompt gained explicit interpretation rules per marker class: HbA1c diabetic-range → low-GI non-negotiable; ALT > 56 → expect to drop with bodyfat; T < 12 nmol/L → double-down on LBM watch; SHBG low → reinforces low-GI; Vit D < 50 → 5000 IU until > 75; hsCRP > 1 → re-check 3mo; never make definitive medical diagnosis, always frame as "consistent with X, recommend GP". One-shot `seedJayBloodMarkers()` startup migration loads Jay's 2026-05-08 Bloodwork Group panel (~40 markers, full lipid + diabetes + liver + hormones + thyroid + vitamins + iron + FBC + prostate). PII is in DB-side encrypted JSON only — PDF source never committed to repo.
- 33: AI session brief (Haiku 4.5, auto on workout open) + post-session reflection. `generateSessionBrief()` in ai-coach.ts returns `{strategy, perExercise:[{exId, cue}]}` via Haiku 4.5 tool use. Context: today's Oura recovery, last night's sleep + stages, yesterday's protein/kcal, user phase + meds + flagged blood markers, today's formula prescriptions, last 2 same-type sessions. ~$0.02-0.03 per session, ~5s. `generateSessionReflection()` returns 1-sentence acknowledgement of completed work. New routes `POST /api/coach/{session-brief,session-reflection}` (JWT). `renderWmOutline` auto-fires brief in background when outline opens, caches result in `state.sessionBriefs[date_session]`, renders strategy block + per-exercise lime cues under each name. `exitGuidedWorkout` fires reflection on exit (cached in `state.sessionReflections`), shown as 6s toast. Crucially: kg/reps come from the deterministic progression formula, NOT the AI — AI only adds the WHY and HOW around the formula numbers.
- 32: Phase-aware coaching + LBM ceiling projection + modal-weight progression. (a) Profile gains `phase` (cut / recomp / lean-bulk / maintenance) and `targetLBMStretch` (kg) — both saved via the existing `PUT /api/state/profile/personal` route with validators. (b) AI Coach system prompt rewritten to read phase from context and adapt advice: cut = LBM preservation, recomp = small simultaneous gain, lean-bulk = controlled surplus (~0.3kg/mo max past 50), maintenance = recovery. Stretch LBM target shifts framing toward multi-month "max muscle at target BF" objective. (c) New `computeMaxLBM(userId)` in ai-coach.ts uses Opus 4.7 with tool use to project conservative/realistic/optimistic LBM ceilings, timeline, phase sequence, and key constraints — factors age, T level, GLP-1 medications, training tier. Saved to `state.maxLBMProjection`. (d) New `POST /api/coach/max-lbm` endpoint + "🧠 Compute my realistic max LBM (AI)" button in Personal Profile section. Result rendered in a generic `modal-info` overlay with quick "set stretch LBM target to AI-recommended value" action. (e) Modal-weight progression fix in `suggestWeight`: replaces "use set 0 as reference" with "use most-frequent (modal) weight as reference; tiebreak heavier." Filters warm-ups + experimental top sets from the rep-range judgement. Fixes the "100×8, 140×3, 120×6, 120×6 → pyramid suggestion" bug. (f) Lower-day exercise swap: `Back Extension` (l7) → `Good Mornings` (no machine needed, barbell hip hinge, same erectors + adds hamstring work). Strength standards multipliers updated to match.
- 31a: Strength Standards card on Track page — classifies each lift into Untrained / Novice / Intermediate / Advanced / Elite based on estimated 1RM (Brzycki from best working set across all logged sessions), per-lift bodyweight multipliers (ExRx + StrengthLevel community data), and age-adjustment (0.5%/yr decay past 30, floor 60%). Includes overall summary tier, per-lift table with 1RM + ×BW ratio + colored tier, and clear footnotes about machine variability + estimation accuracy. Currently male-only; female sex shows a "coming soon" placeholder.
- 31: Past-date training editing + "Where You Stand" peer comparison. (a) Day Detail Training section: each exercise row is now tappable → opens `modal-set-edit` with editable kg/reps/seconds + effort dropdown per set, + Add set, delete-set ×, "Mark exercise as completed" checkbox, and "Remove Exercise From This Day" button. Not-yet-logged exercises from the day's prescribed workout also appear in the list (dimmed) so the user can backfill. Writes through existing `PUT /api/state/exLog/:date`. Critical for keeping the Phase 28 progression engine accurate when corrections are needed retroactively. (b) Body page top: new "Where You Stand" card classifies user against age + sex norms across 6 metrics — Body Fat % (Athletic/Fitness/Average/Above-avg/Obese bands by age), BMI (WHO standard), LBMI (lean / height² — height-normalized muscle index), Visceral Fat (South Asian threshold ≥7 vs European ≥10), 7d-avg Resting HR (Mayo bands), 7d-avg Sleep (NIH 7-9h). Each row shows value + colored category band + segmented bar visualizing position. Falls back to "fill in Personal Profile" prompt when age/height/sex are missing.
- 30a: Today page Key Metrics enhanced — Body Fat row now shows both % AND derived fat mass kg. New Lean Mass row (computed from weight × (1 - BF/100)) shows current LBM, week-rate trend (colored green if holding or up, amber if drop < 0.3kg/wk, red if larger drop), target hold reminder, and sparkline. Sparkline helper `spark()` upgraded: 100×32 (was 80×24), gradient fill under the line, dot at the latest value point — direction is much clearer at a glance.
- 82: Friendlier scorecard + steps target. (a) SCORING: `stepsMetric`/`proteinMetric` (proactive-core.js) now score on PARTIAL per-day credit instead of a binary hit/miss — steps credit = min(1, steps/target); protein credit ramps from 0 at (floor−40g) to full at the floor. A near-miss day (7.5k of 8k; 179g of a 180g floor) counts most of the way instead of zeroing out, so one bad day no longer tanks the grade to F. `hit` (full days) kept for display; `score` = avg per-day attainment; grade bands unchanged. Scorecard text (ai-coach.ts) + visual card (pages.js) now show the `score/100` alongside the day-fraction. (b) STEPS TARGET: `seedJayStepsTargetV1` sets `coachTargets.stepsDaily=8000` (was unset → "no target set", so steps went ungraded). 2 new/updated tests. SW v112.
- 92b (Part 3 engine, "engine first"): KB Swing EMOM progression engine — new PURE UMD module `public/kb-emom.js` (loaded via index.html + sw.js + render-smoke FILES). `KB_EMOM.suggest(exLog, load, {readiness})` derives the next target PER LOAD from that profile's own logged history: Phase 1 (3×10, 4 consecutive FULL → unlock) → Phase 2 EMOM 5×12 climbing +1min/FULL to 8 → Phase 3 (3 FULL at 8×12 → 15 reps, ladder 8→10). PARTIAL drops to rounds-done but never below the last-FULL floor; two PARTIALs at a target → `consolidating`. Readiness <60 caps at the last FULL (null readiness/no-Oura skips the guard → Naveed-safe). New load with no history re-enters at EMOM 5×12 once unlocked (old load's ladder preserved). `pbsByLoad()` = best completed EMOM per weight. Data plumbing (data.js): `getKbLoad`/`setKbLoad` (per-profile `profile.kbSwingLoad`, default 20, new `PUT /api/state/profile/kb-load` jsonb_set route), `kbSuggestion(load)` (auto-applies today's Oura readiness), `getKbPBs()`, `logKbEmomSet(date,{...})` (writes an `{emom,load,rounds,roundsTarget,repsPerMin,durationMin,outcome}` set, outcome derived). 10 engine unit tests (`tests/kb-emom.test.cjs`) cover every rule + 1 render-smoke wiring test. STILL TODO (Part 2, next PR): the full-screen guided EMOM timer + session-start target UI + inline load change + PB card + the KB-swing dynamic normal↔EMOM branch in the guided runner. SW v123.
- 92 (Part 1 of the KB Swing finisher system): added `kb_swing` "Kettlebell Swing (two-handed, Russian)" as the FINAL exercise on both lower days (Lower A after Pallof, Lower B after Suitcase Carry) — `sets:3, reps:'10', rest:60, muscle:'Glutes/Posterior Chain', size:'medium', finisher:true`. Edited global `WORKOUTS` (data.js) + `SESSION_EXERCISE_IDS` + `EXERCISE_NAMES` (programme-shared.js). Template is global → lands on all 5-day-split users (Jay, Naveed, Farukh); per-profile state (load/history/progression) lives in each user's own `state`. Coaching note (form cues + 3/10 pain rule) seeded per-profile for Jay + Naveed only via `seedJayNaveedKbSwingNoteV1()` (guarded `jayNaveedKbSwingNoteV1`). Phase 1 = normal 3×10 weighted sets @ user load (default 20kg). STILL TODO (Part 2/3/5, discussed, not built): EMOM set type + full-screen guided timer (round display, progress ring, per-minute vibrate/beep, 55s warning, wake-lock, pause, end-early), adaptive per-load EMOM progression ladder (FULL/PARTIAL/SKIPPED, readiness guard, per-load PB), session-start target UI + inline load change. SW v122.
- 90a: Day-detail carry-set display fix. `renderDayDetail` (pages.js) Training section filtered sets by `timed?s.seconds:(s.kg||s.reps)`, which missed CARRY sets (Suitcase Carry, `metric:'carry'`) — they log per-side `leftSeconds`/`rightSeconds`, not kg×reps or a single `seconds` — so a completed Suitcase Carry rendered "no sets logged" even though the data was saved (and it counted in the N/N done tally). Added an `isCarry(ex)` branch: filter by `leftSeconds!=null||rightSeconds!=null`, summarise as `L Xs · R Ys`, volume 0 (like timed). Display-only bug — no data was lost. 1 new render-smoke test. SW v121.
- 91: Lower-day restructure (5-day split, jay+naveed+farukh share the global template). RDL (`l2`) moved Lower A → Lower B (position 2, after Hip Thrust, before Leg Press; keeps 3×6–8/120s); Leg Curl (`l4`) moved Lower B → Lower A (position 3, after Leg Press, before Calf Raise; keeps 3×10–12/60s). Final Lower A = `[h1,l1,l4,l6,core_pallof]`, Lower B = `[l5,l2,l1,core_suitcase]`. Edited BOTH the global `WORKOUTS` (data.js) AND `SESSION_EXERCISE_IDS` (programme-shared.js — the single cross-runtime session→exercise map used by classification; server loads the same file). History/PBs are keyed by exercise id (not day/order) so they carry over untouched. Because ordering is global there's no per-profile template, the reorder applies to all 3 five-day-split users. Coaching notes ARE per-profile: `seedJayNaveedLowerNotesV1()` (server/index.ts, guarded `jayNaveedLowerNotesV1`, keyed jay + naveed only — NOT farukh) writes `state.exerciseNotes` for `l2` (RDL restart-90–100kg/straps/+5kg-wk/stop-if-back>3/10) and `u6`+`u7` (golfer's-elbow protocol) in the same `{note,addedAt,source:'coach'}` shape the in-app editor uses. Weekly schedule unchanged (Mon upperA·Tue lowerA·Wed rest·Thu upperB·Fri lowerB·Sat zone2·Sun rest — asserted in the test). 1 new render-smoke test + fixed the pre-existing autoreg test that referenced RDL in lowerA. SW v120.
- 90: Strength-session HR link — overlays the Phase 89 Oura HR time-series onto each logged Forge workout's own time window. New pure `analyzeHrWindow(startMs,endMs,hr)` (walk-analysis.ts, shared by walks + sessions) → `{avgHR,maxHR,minHR,hrDrift,samples}`; `analyzeWalk` refactored to use it. `oura.ts` sync: for each `exLog[date]._session` (startedAt/completedAt epoch ms) within the 60-day HR window, computes session HR + matches an overlapping Oura workout's `calories` → `state.sessionHR[date]={avgHR,maxHR,minHR,hrDrift,durationMin,calories,sessionType}` (merged, client-visible). Coach: new `buildSessionHrContext(state)` block in `buildContext` (last 6 sessions' avg/max/drift/kcal + a rising-HR-at-similar-load fatigue flag). Frontend: `getSessionHR(date)` (data.js) + a "Session heart rate · Oura" card in the calendar day-detail Training section (`renderDayDetail`). Read-only join by time window. Tests: analyzeHrWindow check in `_verify-walk.cjs` + 1 render-smoke. SW v119.
- 89: Oura walk analysis + Engine Trend (extends the existing Phase 6/40 Oura integration; verified endpoint shapes against the in-production oura.ts since cloud.ouraring.com is egress-blocked in the sandbox). NO schema migration — all in the jsonb `state`. (a) HEARTRATE SYNC: `oura.ts` now pulls the `/heartrate` time-series (paginated via `next_token`, `start_datetime`/`end_datetime`) into `state.ouraHeartrate` = `{isoTimestamp: bpm}` — SERVER-ONLY (added to `SERVER_ONLY_FIELDS`, stripped from GET /api/state), pruned to 60 days, written whole-object via `setStateKey`. (b) REAL VITALS: extracts true resting HR (`sleep.lowest_heart_rate`, bpm) + HRV (`sleep.average_hrv`, ms) from the main sleep → `state.ouraVitals[day]={rhr,hrv}` — the existing `recovery.restingHR`/`hrv` were 0-100 readiness CONTRIBUTOR SCORES, not physiological values. (c) WALK ANALYSIS: new pure `server/walk-analysis.ts` (`analyzeWalk`/`analyzeAllWalks`/`isWalkActivity`) computes per walk — durationMin, distanceM, paceMph (distance/1609.344 ÷ hrs; null when Oura omits distance), avgHR, maxHR, hrDrift (final-third avg − first-third avg by TIME, needs ≥6 samples). Runs on every Oura `activity:'walking'` workout + manual walks; stored `state.walkAnalysis[walkId]` (merged, so history accrues past the 60-day HR window). Oura workout store now keeps `distance`. (d) MANUAL WALKS ("Both"): `state.walkLog[date]=[{id,start,end,distanceM,source:'manual'}]` via new dateKeyedRoute `/api/state/walk-log/:date`; `modal-walk` (date/start/duration/miles) + `openWalkModal`/`saveWalkFromModal` (app.js) + `addManualWalk` (data.js) — HR fills in on the next sync if Oura HR overlaps. (e) TOKEN EXPIRY: `ouraGet` attaches `err.status`; a 401 sets `state.ouraTokenInvalid=true` (client-visible) → reconnect banner (`_ouraReconnectBanner`) on the Engine Trend card; cleared on a fresh token (PUT /oura/token) or a successful sync; `/oura/status` returns `tokenInvalid`. (f) UI: new Track-page "Engine Trend · Walks" collapsible — `renderEngineTrend()` charts avg HR on same-pace 2.8–3.2mph walks over time (falling = fitter, via the Phase 66 `_tchartHTML`) + `renderVitalsTrend()` 30-day RHR + HRV mini-charts. Read-only — no writes back to Oura. Tests: `tests/_verify-walk.cjs` (manual, dist-based, 5 checks) + 1 render-smoke. SW v118.
- 88: Daily energy check-in (evening "how was your day"). New `state.energyLog[date] = {level:1-5, tags:[], at}` — a 1-5 energy rating (🪫 Drained → ⚡ Great) + optional "why" tags (`ENERGY_TAGS` = Tired/Foggy/Wired/Stressed/Rested/Strong). data.js: `getEnergy`/`setEnergyLevel`/`toggleEnergyTag`/`_saveEnergy` (persist via new dateKeyedRoute `PUT /api/state/energy-log/:date`). `renderEnergyCard(date, domId)` (pages.js) — a 5-emoji scale + multi-select tag chips, editable for the VIEWED date. Placed on the Food page for `viewDate` (today = evening log; step back with the ‹ › date arrows to backfill) AND in the calendar day-detail modal (`renderDayDetail`, `!isFuture`) so any past day can be marked from the month calendar. Handlers `_setEnergyLevel`/`_toggleEnergyTag` re-render only the card in place (outerHTML by domId). NOT yet wired into the correlation engine / AI coach context — that's the intended follow-up (energy as a subjective input+outcome variable). 1 new render-smoke test. SW v117.
- 87: Eating review + AI food advice on the Food page. LAYER 1 (all users, deterministic, free): new `periodComparison(state, asOf)` (server/nutrition.ts, exported, pure) returns this-week-vs-last (last 7d vs prior 7d) avg calories/protein/carbs/fat + avg weight + days-logged per period. `GET /api/coach/maintenance` extended to also return `recommendation` (from `analyzeNutrition`) + `comparison`. New `renderEatingReviewCard()` (pages.js) on the Food page (today view, below the maintenance card) — a Now/Prev/Δ table for the 4 macros + weight, days-logged counts, and the muscle-governed carb recommendation shown READ-ONLY (a note says apply it from the weekly Coach report; the Apply flow stays in `applyNutritionAdjust`). Reads the same `_maintData` the maintenance card fetches (one fetch, two cards; `_refreshMaintCard` re-renders both). LAYER 2 (owner-only, AI, BYOK): new `eatingAdvice(userId)` (ask.ts, Opus `MODEL`, `EATING_ADVICE_SYSTEM`) → concrete food-level review ("What your logging shows / What to change / Why") over `buildContext`+`buildFullHistory`; new `POST /api/coach/eating-advice` (requireAuth + requireOwnerCheck + aiBudget). Owner-only "🍽️ What should I eat? (AI)" button on the Food page + `runEatingAdvice()` (app.js), result via `formatCoachingReport`, cached in `STATE._eatingAdvice` (mirrors Phase 80 Deep Analysis). 1 new render-smoke test. SW v116.
- 86a: Whey is 20g protein per scoop (user-confirmed). The Phase 86 fix assumed ~24g (set the "2 scoops whey" quick template to 48g); it should be 40g. `fixJayWheyScoop20gV1()` (server/index.ts, guarded `jayWheyScoop20gV1`, keyed jay@afjltd.co.uk, runs right after `fixJayWheyTemplateProteinV1`) recomputes every whey item on 20g/scoop: (1) pure "N scoops whey" quick templates → N×20 (48→40); (2) historical pure-whey food logs → N×20; (3) the live meal-plan shake ingredient (combined whey + blueberries) → 20g/scoop + ~1g berries (1-scoop → 21g), meal total re-derived. `isPureWhey` excludes combined shakes (milk/banana/berry/yoghurt/oats/water) so a real combined shake's protein is never clobbered; `p<200` guard leaves the 10× typo case to Phase 86 (which runs first). Server-only; tsc clean; no SW/frontend change.
- 86: Two nutrition fixes. (a) TISSUE CORRECTION: `analyzeNutrition` (server/nutrition.ts) now applies an 80% correction to the weight-change energy term — `observedTDEE = avgIntake − perDay × 7700 × TISSUE_CORRECTION(0.8)` — because a chunk of short-term scale movement is water/glycogen/gut, not tissue energy; the naive calc over-stated maintenance from a noisy 2-week trend. Backward measurement only (the forward deficit planner in the recommendation keeps full 7700). The maintenance card + recommendation both recalc automatically. The card is fully PER-PROFILE: `GET /api/coach/maintenance` reads `req.userId`'s own state (foods/weightLog/calorieLog/foodComplete) + `_formulaTDEE()` reads the logged-in `getActive()` profile — no hardcoded user. `_verify-nutrition.cjs` check 3 fixture deepened (1kg→2kg/2wk loss) so the muscle-first "ease → add carbs" path still has deficit headroom under the lower corrected maintenance; all 7 checks pass. (b) WHEY TEMPLATE FIX: `fixJayWheyTemplateProteinV1()` (server/index.ts, guarded `jayWheyTemplateFixV1`, keyed jay@afjltd.co.uk) corrects the user's saved "2 scoops whey" quick template protein 480g→48g (a 10× typo) AND any historical `state.foods` entry that used the wrong value (day totals are summed live from entries, so fixing the entry recalculates the day). Guard: only touches a whey/scoop entry with protein ≥200g (no real single food is that high, so a correct 48g entry is never affected). Server-only; tsc clean; no SW/frontend change.
- 85: Maintenance calories (TDEE) readout on the Food page. Surfaces the Phase 48 `analyzeNutrition` engine directly to the user (previously only fed the Sunday coach report). New thin `GET /api/coach/maintenance` (coach-settings.ts, `requireAuth` only — all users, no AI/aiBudget) runs `analyzeNutrition(state, ukToday())` and returns `{observedTDEE, ouraTDEE, avgIntake, rateKgPerWk, loggedDays, completeDays, windowDays:14, confidence, confidenceReason}`. MEASURED maintenance = avg logged intake − weight-trend energy (least-squares slope × 7700 kcal/kg) over a rolling trailing 14 days; high-confidence only at ≥10/14 logged days + a readable weigh-in trend (else low-conf with a "keep logging" reason). Frontend (`pages.js`): new `renderMaintenanceCard()` shown on the Food page (today view, below the macro card) — headline number + a MEASURED/FORMULA badge, the weight-trend reasoning line ("averaging X kcal and losing Y kg/wk → maintenance ≈ Z"), a 3-way Measured/Oura/Formula comparison row, and the confidence note. `_formulaTDEE()` computes the Mifflin (targets.js `computeTargets().tdee`) estimate CLIENT-side as an instant cold-start fallback + cross-check (shown before the fetch returns / when offline / until enough days logged). `loadMaintenance()` fetches once per session and swaps `#maint-card` via outerHTML. Because it uses a ROLLING recent window it reads existing history immediately (not "from day one" of the account) and reflects current bodyweight, not who the user was months ago. 1 new render-smoke test. SW v115.
- 84: Chai + honey quick-log templates (jay@afjltd.co.uk). `seedJayChaiHoneyQuicklogsV1()` (server/index.ts, guarded `jayChaiHoneyQuicklogsV1`, runs right after `seedJayDaalFibrePlanV1`) APPENDS two per-serving quick-log meals to the LIVE `state.mealPlan.meals` (doesn't rewrite the plan, so it composes with the daal+fibre plan and survives a macros recompute): `ql-chai` "Asian tea (chai)" — 1 cup, 80 kcal/4P/10C/2F, gi moderate (assumes semi-skimmed milk + 1 tsp sugar; user has up to 3/day) and `ql-honey` "Honey" — 1 tsp, 21 kcal/0P/6C/0F, gi high (user has up to 2 tsp/day). Per-serving = one tap per unit consumed, or use the Phase 25 portion stepper. Idempotent (skips ids already present). No target/engine/schema change. Server-only; tsc clean.
- 83: Between-exercise filler + 45s plank drill (guided workout). (a) POST-EXERCISE REST: after an exercise's FINAL set, the guided runner now runs one more rest countdown with the same filler panel (remaining rehab accessory, else a mobility hold) BEFORE the exercise-complete summary — a standard slot to knock out accessories/mobility between exercises (user chose "dedicated rest + filler"). New `_wmEnterPostExerciseRest(ex)` + `wmEndPostExerciseRest()`; the three last-set handlers (`wmRecordEffort`/`wmRecordTimedEffort`/`wmRecordCarryEffort`) route through it instead of jumping straight to `renderWmExerciseDone`. Skipped after the LAST exercise (`_wmNextPendingIdx===-1`) so it never adds friction to finishing. `renderWmRest`/`updateWmRest` are `wm.postExercise`-aware (no next-set autoreg panel; footer reads "Next up: <ex>"; button "SKIP REST · CONTINUE →" / "CONTINUE →"). `postExercise` cleared in `wmStartNextSet`/`wmNextExercise`; persisted via the existing wm autosave so a mid-rest resume restores it. (b) PLANK MOBILITY DRILL: new `mob_plank` in `MOBILITY_DRILLS` (`metric:'time'`, `reps:'45s'`, `capSeconds:90`, own id so its rest-gap holds log separately from the main-lift `u9` Plank). The mobility fallback (`_wmMobilityFallbackHTML`) generalised from the hardcoded Deep Squat to a `MOB_DRILLS` list with a 2-chip picker (🧘 Deep Squat / 🧱 Plank); `wm.mobDrillId` holds the selection, `wmSelectMobDrill(id)` switches + re-renders only the panel. `wmRestMobilityDone` logs to the selected drill's exercise (own seconds-volume + `suggestTime` duration progression) and credits the restMobility 0/7 tile with the drill's stretch id (`rm_deep_squat`/`rm_plank`, added to the restMobility routine). 3 new render-smoke tests. SW v114.
- 82a: Protein grades the last 7 COMPLETED days (excludes today). `weeklyReport` (proactive-core.js) now passes `proteinMetric` a `_last7(today − 1)` window (d(-7)..d(-1)) instead of the shared today-inclusive `_last7(today)` — today's food is still being logged, so a partial today unfairly dragged the protein score down. The other metrics (steps/sleep/weight) keep the today-inclusive window. 1 new test (7 completed full days = 100, a partial 0g today ignored). SW v113.
- 81: Stall detection ignores discontinued lifts — `detectStalls` (proactive-core.js) flagged any lift held at the same weight for 3+ of its last 6 sessions regardless of recency, so an exercise dropped from the program (e.g. neck flexion, last trained ~6 weeks ago) still showed as a "stall" in the coach report indefinitely. Added a recency guard: skip a lift whose most recent session is >28 days before the latest training day in the log (via `_dayIdx`). Current stalls still flagged. 1 new test. SW cache unchanged (server reads same shared file; frontend logic-only).
- 80: Pattern discovery + on-demand Deep Analysis (coach intelligence). SMALL — automated pattern mining: `minePatterns(state)` (proactive-core.js) scans behaviour→outcome pairs (same-day AND next-day lag) across all logged streams (sleep hrs/deep/REM, steps, readiness/HRV/RHR, kcal/protein/carbs, training volume, bedtime, water), computes Pearson r via the existing helper, requires n≥14 AND |r|≥0.4, dedupes the 5 fixed correlations, ranks by |r|, returns top 3. Added as `discovered[]` on `computeCorrelations` (runs nightly, cached on `state.correlations`); `formatCorrelations` emits a DISCOVERED PATTERNS block with an EXPLORATORY caveat (multiple-comparison + correlation≠causation). System prompt gains a rule: treat discovered patterns as hypotheses, raise at most one gently, only act if plausible AND |r|≥0.5. Stats are deterministic in code — the LLM only interprets (LLMs confabulate patterns from raw data). MEDIUM — on-demand Deep Analysis: `deepAnalysis(userId)` (ask.ts, Opus/`MODEL`) over buildContext+buildFullHistory → "3 biggest levers right now + one 2-week n-of-1 experiment"; new `POST /api/coach/deep-analysis` (owner, aiBudget); "🔬 Deep Analysis" card on the Coach page (`runDeepAnalysis()` in app.js, result via `formatCoachingReport`, cached in `STATE._deepAnalysis`). 2 new tests (miner finds ranked lagged+same-day patterns; weak/underpowered candidates excluded). Server tsc clean; SW cache v111.
- 79: RECENT CHANGES timeline in the coach context. The coach saw trends (week-over-week, correlations) but not WHEN the user deliberately changed something, so it couldn't attribute before/after impact to a specific intervention. New `buildRecentChanges(state)` (ai-coach.ts) assembles a newest-first dated timeline from markers already in state (NO schema change): plan/cut start, program set (`programmeStartDate` + `programmeLabel`), phase set (`activePhase` start + target/goal), deload cadence set (`deloadConfig.updatedAt`/anchor), meal-plan updates (`lastMealPlanRegenAt` + `mealPlan.name` which encodes the macros), and injuries flagged/resolved. Each line shows date + "Nd ago". Wired into `buildContext` just before WEEK-OVER-WEEK (so report + chat + brief get it), top 12 entries. System prompt gained an INTERPRETATION rule: attribute metric movement to the user's own interventions ("since the meal-plan change on <date>, carbs/energy/volume moved X — working / not yet"), not just to time. Server-only; tsc clean; no schema/frontend change.
- 78: Strength-since-start baseline in the weekly report. `buildTrainingDetail` only feeds the coach the last 4 sessions per lift, so the weekly REPORT was blind to strength gained since plan start — the prime evidence muscle is preserved on a cut. The chat/Ask-Forge already had it (via `buildFullHistory`'s per-lift first-vs-current block) but the report (buildContext only) did not. New exported `buildStrengthBaseline(state)` (ai-coach.ts) — per-lift first-ever vs current top set + relative strength (kg/kg bodyweight, ↑/↓/= direction), readable names via `exerciseName()`, ≥3-session lifts, top 16. Added to `buildContext` (after buildTrainingDetail) so report + chat + session brief all get it. The duplicate per-lift block was REMOVED from `buildFullHistory` (ask.ts) since chat/ask prepend buildContext — so it's emitted exactly once, no duplication. Server-only; tsc clean; no schema/frontend change.
- 77: Chat bug fix — `POST /api/coach/chat` (coach-settings.ts) validated EVERY thread message ≤500 chars, including assistant turns. The frontend echoes the recent thread (incl. the model's own long prior replies, stored up to 4000 chars) back each turn, so once the coach gave a >500-char answer, every follow-up hit "Keep each message under 500 characters" (400) → `sendCoachChat` reverted the optimistic user turn → chat appeared frozen on the previous answer. Fix: the 500 cap is a USER-INPUT limit only (reject if over); assistant turns are truncated to 4000 (never rejected). Server-only; no schema/frontend change.
- 76: Daal + fibre meal revision (user-directed, jay@afjltd.co.uk). `seedJayDaalFibrePlanV1()` (server/index.ts, guarded `jayDaalFibrePlanV1`, runs after the rebalanced plan = final word) overwrites `state.mealPlan`: dinner → "Chicken, Daal & Rice" (150g chana daal + 50g basmati replace the rice, big salad removed); breakfast drops the 150g yoghurt; evening drops psyllium, adds a kiwi, trims almonds 30g→15g; adds 3 quick-log templates (banana / dates / standalone psyllium+water) as extra plan meals. RECONCILED against the actual live plan + flagged to user (peas + CoQ10 stay in pre-workout where they live; the almond change applies to the evening meal). Forge has NO fibre field (only in comments) — user chose option B (apply without fibre), so fibre omitted. Targets re-pointed to 2270/183P/210C/64F. Trimmed per user (pre-workout basmati 150g→100g, dinner chicken 130g→120g): core 5 meals ≈189P/168C/64F/2064; +3 quick-logs on training days ≈190P/218C/64F/2251 (under the 2,300 ceiling). No engine/schema change.
- 75: Rebalanced Abdul's cut meal plan — same ~2,200 kcal, macros shifted 244P/145C/77F → 200P/185C/72F. Data review showed 239g protein was above every guideline (~3.1g/kg lean), pushing urea up, crowding out training carbs, and hard to finish on a GLP-1. `seedAbdulRebalancedPlanV1()` (server/index.ts, keyed jay@afjltd.co.uk, guarded `abdulRebalancedPlanV1`, runs last among the Abdul meal-plan writers) overwrites `state.mealPlan` with the rebalanced 5 meals (recognisable foods — kept peas/psyllium/big salad/CoQ10; post-workout shake dropped 2 scoops→1, 49g→24g protein; more basmati pre/post + more oats AM for low-GI training-timed carbs), and re-points `pf.macros`/`pf.dynamicTargets` (2200 kcal, 200/185/72), lowering `proteinFloor` 200→180 (+activePhase/coachTargets floors). No engine/schema change.
- 74: Morning serums — the AM branch of `getSkinVisibleItems` (data.js) now renders `type:'serum'` products (slot `am`/`both`), applied **thinnest-first** by concentration between Vitamin C and moisturiser (cleanser → vitamin-c → serums → moisturizer → spf). Previously the AM loop only surfaced cleanser/vitamin-c/moisturizer/spf, so an AM-slot serum (e.g. Alpha Arbutin 2%, Niacinamide 10%) was silently dropped from the morning list AND excluded from PM (PM serums require `slot==='pm'`) — it vanished entirely. Mirrors the existing PM serum ordering; no PM change, no retinol-night logic in AM (there's no retinoid in the morning). Fixes a real user hit: Jay moved Arbutin + Niacinamide to AM (cleanser→VitC→arbutin→niacinamide→moisturiser→SPF) and they disappeared. 1 new test (AM order cleanser→VitC→arbutin(2%)→niacinamide(10%)→moist→SPF + PM serums unchanged + AM serums don't leak into PM). SW cache v110.
- 73: Read-only skincare WEEK VIEW (owner-only) — a Mon–Sun strip on the Today page mirroring the training week strip, so you can see which products are due which days (essential now that Phase 72 specific-day scheduling lands different products on different nights). New `renderSkinWeekStrip()` (pages.js, above `renderSkinChecklist` in `renderSkinSection`) with its OWN view-date state (`_skinViewDate`/`getSkinViewDate`/`shiftSkinWeek(±1)`/`goToSkinToday`) — deliberately NOT workout.js's `viewDate` (that drives the Train page; kept fully separate). Monday-anchor math + week-label logic copied verbatim from `renderWeekStrip`. Each day cell shows the weekday initial, date, an AM·n/PM·n load marker, and a 🌙 dot on retinoid nights; tapping a cell (`toggleSkinDayExpand`) expands that day's AM/PM product lists. PURE REUSE of the engine: every day's products come from `getSkinVisibleItems(key)`, so retinol-night branching (arbutin/niacinamide dropped, cicaplast shown), mask suppression, and specific-day scheduling all render correctly with ZERO new scheduling logic. Read-only — no toggles, no logging, no server calls (reads in-memory STATE); the existing today-only checklist stays unchanged below it. Nav re-renders ONLY the `#skin-week` node (`refreshSkinWeek`), not the whole Today page. Tier B (interactive past-day logging) explicitly NOT built. 1 new test (7-cell grid + week nav + specific-days mask in correct expanded day + retinoid-night marker/suppression + Train-page view-date untouched). SW cache v109.
- 72: Specific-weekday scheduling for skincare products. The frequency picker only offered intervals (daily / every 2-3-4 days / weekly), which couldn't express "specific nights" (e.g. a mask used Mon+Thu). New `frequency:'specific-days'` value + `product.days` = array of day-of-week ints (0=Sun..6=Sat). ENGINE: `skinDueOn` (data.js) gains ONE branch before the interval math — `specific-days` → `product.days.includes(dow)` — deterministic per date, so `getSkinVisibleItems`/`getSkinCompliance`/the conflict engine all work unchanged (the mask-on-retinol-night suppression still composes: a specific-days mask landing on a retinol night is still auto-hidden). New `_sanitizeSkinDays()` (unique ints 0-6, sorted) applied in `addSkinProduct`/`updateSkinProduct`. UI: `modal-skin-edit` gains a "Specific days" option + a 7-chip weekday selector (M T W T F S S, Mon-first) shown only when selected; `openSkinProductEdit` populates chips from `product.days`, `saveSkinProduct` reads them back (requires ≥1 day), `onSkinFreqChange`/`toggleSkinDayChip`/`renderSkinDayChips`/`_updateSkinFreqOptions` in app.js. GUARDRAIL: retinol-type products AND `skn-cicaplast` do NOT offer specific-days — the tret ladder (`PHASE_FREQ`) rewrites their frequency on phase-advance, so a hand-set value would be silently wiped; `_updateSkinFreqOptions` hides/disables the option for them (on open + on type-change). LABEL: `_SKIN_FREQ_LABEL` gains a `5x-week` entry (fixes a pre-existing raw-string render) + new `_skinFreqLabel(p)` renders specific-days dynamically ("Mon, Thu"). NOT built: true floating "any N of 7" (audit showed it breaks the per-day compliance denominator) — "X times/week" is only ever a UX shortcut over specific-days. No server/schema/compliance change (the `/skin-care` route stores the whole object as JSON, no field validation). 1 new test (specific-days due-logic per weekday + intervals/5x-week untouched + sanitizer + dynamic label + conflict-engine visibility). SW cache v108.
- 71: Range-aware activity metrics on the Track "Compare Any Two Dates" card. The card compared two SINGLE dates for every metric — fine for body composition (weight/BF/lean at date A vs B is a true two-point comparison) but noise for sleep/steps (7.1h on one night vs 4.7h on another tells you nothing). New pure `_rangeActivityStats(from,to)` (public/pages.js, next to `_snapshotAtDate`) walks each day in the range and aggregates **average + total over days-with-data** (count returned so sparse logging is visible): Sleep (`sleepLog[d].hours`), Steps (`stepsLog[d]`), Calories eaten (`foods[d][].cals` — new), TDEE burned (`calorieLog[d].total`), avg daily energy **Balance** (eaten−tdee, over days with both; green=deficit), and strength training — **Sessions** (strength-only: a logged non-`_` lift with values that isn't rehab/cardio/mobility via `_isRehabOrCardio`+`exObj.mobility`), **Volume** (Σ `computeSessionVolume`, total + avg/session), **Time** (Σ `_session.totalDuration` where recorded, total + avg/session). `renderCompareSnapshot` drops the old per-date Sleep/Steps/TDEE `_row`s, keeps the body-comp A/B/Δ table, and appends an "Over this range · N days" block below (label|value layout; each metric shows "—" when no data). Date loop uses local date components (TZ-safe, no UTC round-trip); reversed pickers swap internally; loop capped at 2000 days. Frontend-only, no server change. 2 new tests (aggregation math incl. rehab-day exclusion + reversed/empty ranges; full render drops per-date rows and shows the range block). SW cache v107.
- 70: Configurable deload cadence — deload weeks decoupled from the training rotation and opened to every program. Before this, scheduled deloads were hardcoded to `upper-lower-5d-fixed` (every 5th week off `programmeStartDate`, which also anchors the day rotation — so you couldn't move a deload without shifting training days). New `profile.deloadConfig = {enabled, everyWeeks (1–52), anchorMonday}` — a program-agnostic cadence with its OWN anchor. (a) ENGINE: `_manualDeloadInfo(dateStr)` (weeks since `anchorMonday` % `everyWeeks` === 0, forward-only) + `_deloadInfoFor` (config wins over the built-in 5-day schedule) + `_deloadActiveForUser`; `_isDeloadDate` (the single chokepoint feeding `_effectiveSets`/`isDeloadWeekToday`/the 60%-2-set progression overlay/the non-deload reference filter/`detectStall`) is now config-aware, so the whole progression engine picks it up with no per-call-site changes. Deload prescription unchanged (60% of last non-deload modal weight, 2 sets, rehab/cardio/timed exempt). (b) ANALYTICS FIX: `computeSessionScore` excludes deload weeks from the 4-week volume baseline (they're ~60% by design — including them made normal weeks read inflated) and returns a `deload:true` flag so the recap/coach treat a low deload score as planned recovery, not regression. (c) COACH: `buildContext` (ai-coach.ts) reads `deloadConfig` first (falls back to the 5-day schedule) — "Deload cadence: every N weeks… THIS IS A SCHEDULED DELOAD WEEK / next deload in N weeks", so a light week is never misread as regression. (d) UI: More → Training Schedule gains a "Deload weeks" card (enable toggle + every-N-weeks + first-deload-Monday date picker + live "next deload in N weeks" preview); `getDeloadConfig`/`saveDeloadConfig` (data.js), `loadDeloadConfigUI`/`saveDeloadConfigFromUI` (app.js), new atomic `PUT /api/state/profile/deload-config` (jsonb_set, JWT, validates everyWeeks 1–52 + anchorMonday date). (e) SEED: one-shot `seedJayDeloadCadenceV1` sets Jay's config to every 8 weeks anchored to Mon 2026-08-03 (this week = deload). 3 new tests (program-agnostic config deload fires + math + disabled no-op + baseline-exclusion). SW cache v105.
- 38: Training system comprehensive update — eight changes. (a) EXERCISE SWAP: lower day drops Good Mornings; adds `l7_cable_pull` Cable Pull Through + `l8_rev_hyper` Reverse Hyperextension; `core_dead_bug` Dead Bug added to BOTH upper (after Plank) and lower. Strength Standards table updated. (b) INJURY SYSTEM: `state.injuries` (object keyed by id) — `{name,bodyPart,severity,affectedExercises[],status,notes}`. `PUT /api/state/injuries` (jsonb_set, JWT). data.js helpers `getInjuries/getActiveInjuries/isExerciseInjured/getInjurySeverity/getInjuryForExercise/addInjury/updateInjury/resolveInjury/deleteInjury`. Progression engine wraps `suggestWeight` → `_suggestWeightCore` + `_applyInjuryToSuggestion` (mild ×0.80, moderate ×0.65, severe = hold/see-doctor). Red injury banner + per-lift badges on workout outline. More page "Injury Management" section + `modal-injury-edit` (affected-lift checklist). Startup migration `seedJayInjuryV1` flags a mild lower-back niggle on RDL + hip-hinge accessories. (c) REST-TIME RECORDING: set model gains `setStartedAt/setCompletedAt/restStartedAt/restCompletedAt/actualRestSeconds/prescribedRestSeconds`; exercise-level `exerciseStartedAt/CompletedAt/totalExerciseDuration`; session-level `exLog[date]._session` `{startedAt,completedAt,totalDuration}`. Guided-mode rest timer now colour-coded green→amber→red. (d) SESSION TIMING: removed hardcoded "16:00"; `profile.sessionTimes` per day-of-week (default Mon/Wed/Fri 16:00, Sat/Sun 14:30, Tue/Thu off) via `PUT /api/state/profile/session-times`; More page "Training Schedule" section; Today card reads the per-DOW time. (e) WARM-UP PROTOCOL: first working set of a session shows a warm-up prompt (~50% load, rounded to 2.5kg). (f) PER-LIFT AI DETAIL: `buildTrainingDetail()` adds a TRAINING DETAIL block (per-lift last-4-session tables w/ sets, effort letters, avg rest) + TRAINING ANALYSIS RULES in the system prompt. (g) SLEEP-PERFORMANCE: `buildSleepPerformance()` adds a SLEEP VS PERFORMANCE CORRELATION block (last 8 sessions, sleep+readiness→volume/effort) + prompt rule. (h) ACTIVE INJURIES block fed into coach context. buildContext now skips `_`-prefixed exLog keys so `_session` doesn't inflate counts.
- 41: Stretching / mobility routines (owner-only, jay@afjltd.co.uk). New `STRETCH_ROUTINES` constant (8-stretch morning + 7-stretch evening) in data.js. `state.stretchLog[date]={morning,evening}` with `completedStretches[]` + `skippedStretches[]` and a `completed` flag; `state.stretchStreak` per type. New `PUT /api/state/stretch-log/:date` (date-keyed jsonb_set). `isStretchUser()` wraps `isOwner()` and gates every render path. Today page: morning + evening cards (state + time-aware: 🌅 pre-12pm, ⚠️ after; 🌙 17-22, ⚠️ after 22). More page: "Stretching · Mobility" section with 7d compliance, streaks, 7-day dot grid. Fullscreen guided session overlay (`stretchMode` div) — pre-session outline → active screen (countdown for timed stretches, +rep button for rep stretches, auto LEFT→RIGHT for sided stretches with vibrate cue) → 10s rest transition → completion screen with feel selector (better / same / unsure). Progress saved per stretch so interrupting doesn't lose work. AI Coach: `buildStretchContext()` emits a STRETCHING COMPLIANCE block (compliance, streaks, most-skipped, trend vs prior 7d, on-time counts). System prompt gains a `## Mobility & stretching` report section between Recovery and Skin, plus rules: morning <4/7 → call out hip-flexor + pelvic-tilt by name; evening <4/7 + HRV<20 → 4-7-8 + legs-up-the-wall therapeutic framing; both ≥6/7 → ask user to notice back-pain/sleep improvements; streak ≥7d → "postural change permanent at 4-6 weeks"; <5/7 → eligible for Priority Action with named stretches.
- 40: AI Coach + integrations comprehensive update — nine sections. (a) TOKEN ENCRYPTION: new `server/token-crypto.ts` (`readToken`/`writeToken`/`isEncryptedToken`) — Oura PAT + Withings access/refresh tokens now AES-256-GCM encrypted at rest (stored as `v1:` ciphertext in the same fields, transparent lazy migration on next sync). Withings OAuth callback rejects state older than 10min (`?withings=expired`). "Reset All Data" clears Oura/Withings tokens + calls disconnect endpoints. (b) NEW SUGGESTION TYPES: `training-swap` (writes `state.exerciseNotes`), `injury-flag` (server-side flag/resolve on `state.injuries`), `fasting-note` + `supplement-reminder` (post in-app notifications) — apply handlers in coaching.ts, tool-schema enum + system-prompt entries, coloured type badges in `renderSuggestions`. (c) NOTIFICATIONS: `state.notifications[]` (max 10, auto-expire), `PUT /api/state/notifications/:id/read` + `DELETE /api/state/notifications/expired`, bell+badge in Today header opening a modal panel. (d) CRON: Wednesday 14:00 Mounjaro reminder + daily 08:00 missed-critical-supplement check (both write notifications); Sunday report failures now record a non-fatal `type:"error"` entry instead of silently dropping. (e) SYSTEM PROMPT: COACHING PRINCIPLES (10) + TONE + 5-section report structure + max-5-suggestions limit — kept generic/context-driven, NOT hardcoded to one user. (f) MAX LBM: medical-lever framing (testosterone/HbA1c/age/GLP-1, no-TRT vs TRT scenarios). (g) SESSION BRIEF: today's nutrition + water + active injuries + Wednesday Mounjaro note fed into context. (h) COACH PAGE: transformation summary card (days in, weight/fat/LBM, sessions, streak, retinol phase) + next-report countdown + generate-now. Section 3's comprehensive-context items were already covered by Phases 30/38/39 blocks.
- 39: Nutrition system comprehensive update — ten sections. (a) DYNAMIC TARGETS: `calculateDynamicTargets()` (Mifflin-St Jeor, reads `profile.personal` age/height/sex) recalculates calories + macros on every weight log; `profile.dynamicTargets` holds rest/upper/lower values; Today/Food pages read `getDynamicTargetForDate()`; 5kg milestone toast; `PUT /api/state/profile/dynamic-targets`. (b) FASTING TRACKER: 12:00-18:00 window, `state.fastingLog` recomputed from logged food times on every food change, `getWindowCountdown/getFastingStreak/isFastingWindowOpen`, fasting card on Food page (before/open/after phases + progress arc), streak chip on Today, window-break flagging. (c) MOUNJARO MODE: Wed injection-day + Thu post-injection banners on Food page, Nausea Mode priority-food modal, `state.mounjaroLog` with injected/time/sideEffects, Wednesday injection tick + side-effect selector on Today. (d) PER-MEAL PROTEIN: 40g threshold badges on meal-plan cards, protein-distribution bar + score on Food page. (e) SUPPLEMENTS: enhanced existing Phase 19 system — `timing`/`withFood`/`critical`/`frequency` fields, Today checklist grouped by timing with critical ⚠️ + missed-critical summary; `seedJayNutritionSuppsV1` merges canonical 9 onto existing list. (f) WATER: replaced cup tracker with ml-based `state.waterLog` (entries[], progress circle, +250/500/750/custom quick-add, undo, 3L/3.5L gym target, EOD reminder); old cups migrated ×250ml. (g) BLOOD-MARKER NUTRITION: keyword `estimateGI()` low-GI flagging on food log + meal-detail ingredient badges; AI prompt blood-marker nutrition rules. (h) TRAINING-NUTRITION: pre-workout (90/60min) + post-workout protein-window + muscle-group banners on Food page using `profile.sessionTimes` + exLog `_session`. (i) AI CONTEXT: `buildNutritionContext()` adds FASTING / WATER / MOUNJARO / PROTEIN DISTRIBUTION blocks + nutrition system prompt rules. (j) New endpoints `PUT /api/state/{fasting-log,mounjaro-log,water-log,supplement-log}/:date` + `/profile/dynamic-targets`.
- 30: Period comparisons + TDEE fix — three coach context blocks plus a new Track page card. (a) Bug fix: coach was told `active_calories` was TDEE; corrected — `total_calories` from Oura ≈ TDEE (BMR + active). Both labelled clearly in context. (b) SINCE PLAN START block: weeks-in, weight/BF/LBM absolute deltas from `profile.startWeight/startBF/startLBM`, plus a preservation-status verdict (EXCELLENT / GAINING / minor loss / concerning loss). (c) WEEK-OVER-WEEK block: last 7d vs prior 7d, aggregated `periodStats()` helper computes avg weight/BF, training sessions, sleep hrs, steps, TDEE, food kcal, protein with side-by-side + delta. (d) MONTHLY ARC block: last 30d vs prior 30d, only renders when ≥30d of data exists. System prompt rule additions: every "This week" section must cite SINCE START framing + WEEK-OVER-WEEK direction (accelerated/held/slowed/stalled). (e) Frontend: new "Compare Any Two Dates" card on Track page (between Body Comp Trend and the first metric card) with two date pickers and a side-by-side snapshot of weight/BF/LBM/fat/muscle/visceral/sleep/steps/TDEE, color-coded delta column. Default range = plan start → today. New `_snapshotAtDate()` JS helper.
- 42: Family onboarding — app opened to family members (male/female, incl. under-18). (a) TARGETS ENGINE: new `public/targets.js` pure engine (`computeTargets`/`computeWaterTarget`) — deficit/surplus by profile phase (cut −20% / recomp −10% / maintenance 0 / lean-bulk +10%), protein by g/kg bodyweight per phase, calorie floors 1400F/1600M, water 35ml/kg (+500 gym days), HARD GUARD: under-18 never gets a deficit. Jay keeps exact pre-42 numbers via `profile.targetOverrides` (`seedJayTargetOverridesV1` migration: fixed 350 deficit, LBM protein w/ 200g floor, 2400 floor, 3.0/3.5L water) — parity proven by test sweep. (b) RECOMMENDATION: `recommendGoal(stats)` rule table (BF bands + BMI + LBMI, female variants) → recommended phase + band-based reasons; minors and BMI<18.5 are never offered a cut; Where-You-Stand band data moved to targets.js as the single source. (c) WIZARD: 5-step first-login onboarding (about you → recommendation+goal → training → food → confirm computed plan) replaces the old type-your-own-calories flow; writes `profile.personal`/`programId`/`eatingWindow`/`foodPrefs`/first weigh-in + plan dates. (d) PROGRAMS: WORKOUTS gains `full` (gym Full Body 3-day Mon/Wed/Fri) + `home` (dumbbell/bodyweight) templates sharing exercise IDs with upper/lower so history transfers; `PROGRAMS` registry keyed by `profile.programId` (default `upper-lower-4d` = zero change for Jay); `getAllExercises()` replaces hardcoded upper+lower spreads; calibration mode (FIND WEIGHT badge + banner + in-set hint) for no-history lifts; female strength-standard multipliers shipped (placeholder removed). (e) DE-JAY: Mounjaro UI gated on meds/supplements via `_userOnMounjaro()` (was: every Wednesday for everyone); eating window now `profile.eatingWindow` (wizard default OFF; missing field = legacy 12–20 on); Calorie Stage Guide computed from own profile via targets engine (cut phase only); `isOwner()` name-based fallback removed. (f) OPS: owner-run password reset (`POST /api/admin/reset-password` + More page admin button); offline-safe saves — failed writes queue in localStorage (latest-wins per endpoint), flush on reconnect/30s/tap, amber unsynced badge in top bar (replaces silent `catch{}`); `JWT_SECRET` validated at startup; startup migrations now run sequentially (await chain). Tests: `npm test` (node --test, zero deps) — 25 tests incl. Jay-parity sweep + recommendation guards.

- 43: Security hardening (server-side; no new features). (a) SECRETS: `SERVER_ONLY_FIELDS` in state.ts (`coachingKey`, `ouraToken`, `withings`, `withingsOAuthState`) — GET /api/state strips them via deep clone; full PUT /api/state copies them back from the DB row so a client write can never read, delete, or overwrite them (offline-queue full-state flush verified compatible — frontend never reads these fields, status endpoints used instead). NOTE: `coachMemory` doesn't exist in state — closest real field is the non-secret `coachingReports`. (b) AI BUDGET: `aiBudget()` middleware on all 7 Anthropic-calling routes (test, generate-now, recompute-macros, session-brief, session-reflection, max-lbm, regenerate-plan) — `state.aiCallLog["YYYY-MM-DD"]` (UK date = UK-midnight reset) incremented via single-statement jsonb_set UPDATE…RETURNING (atomic, increment-then-check), 40/day limit → 429, old day-keys pruned on first call of a new day, fails open if bookkeeping errors. generate-now cooldown 60s → 30min. (c) INVITE-ONLY SIGNUP (43.5): owner generates one-time invite links from More → Admin (`POST/GET/DELETE /api/admin/invites`, `InviteCode` table, codes burn on use with usedBy audit; unused invites revocable). Links land on `/login.html?invite=CODE` which pre-fills the signup tab. Door-lock rule: signup is gated once env `INVITE_CODE` is set OR any invite row exists (rows persist after use, so creating the first invite permanently locks the door); a signup passes with the shared env code or a valid unused one-time code; neither configured = open (dev). Invite field on signup form. (d) ACCOUNT DELETE: DELETE /api/auth/account requires `{password}`, bcrypt-verified; frontend prompts for it and no longer clears localStorage on failure. (e) DROPPED `AccessToken` model + table (hand-authored migration `20260610000000_drop_access_tokens`; PAT system dead since Phase 34; no code referenced the model). (f) RMW→jsonb_set: coach key save/delete, max-lbm projection save, cron Sunday error-report recording (atomic array prepend) — the remaining whole-state writes are tracked, not converted. NOTE: `INVITE_CODE` env is now optional — generating the first invite link locks signup by itself; the env var acts as an additional shared master code if set.

- 44: Recovery gate override + calibration — the Phase 28 gate (readiness <60 / HRV↓3d) is now ADVISORY, because Oura under-scores shifted sleep schedules (Jay sleeps 3-4am). (a) PRE-SESSION FEEL: opening a workout asks "How do you feel? Strong/OK/Tired" BEFORE any prescription or recovery warning renders (unanchored answer); `state.sessionFeel[date]` via new dateKeyedRoute `/api/state/session-feel/:date`. (b) ADVISORY GATE: when the gate fires, a choice screen shows the reason + feel and offers "Train as planned" (normal progression via new `effectiveRecoveryGate()` wrapper — all 4 call sites converted) vs "Take it easy" (hold, as before). Choice logged to `state.recoveryOverrides[date]` = {readiness, hrvDown3d, feel, choice, sessionType, deloadHolds} via `/api/state/recovery-overrides/:date`. Outline banner reflects the choice. New wm modes `feel`/`gate` with resume support. (c) STALL DELOAD: deload suggestion now carries `holdKg` (the stalled weight); set screen offers "Hold Xkg instead" + cut-phase reframe copy ("holding weight while bodyweight drops IS progress"); choice logged to `recoveryOverrides[date].deloadHolds`. Progression math unchanged. (d) SESSION SCORE: `computeSessionScore()` — volume (kg×reps; seconds for holds) as % of same-session-type 4-week average + effort mix — stored in `exLog[date]._session.score` at workout completion. (e) COACH CALIBRATION: `buildCalibrationContext()` block (last 8 gate firings: readiness/feel/choice → score → injury-within-7d from Phase 38 createdAt) + "## Recovery calibration" report section instructing the coach to answer explicitly whether readiness <60 predicts weak sessions FOR THIS USER and whether the personal threshold should move; session brief gets today's feel + gate choice + last-3 override outcomes ("last time you trained through at readiness 55 you scored 104%"). No new AI calls during workouts.

- 45: Ask Forge (owner-only Q&A) + missing coach data + context preview. (a) DEXA CONTEXT: `buildDexaContext()` — every `state.dexaScans` entry in the weekly report context with key values, plus the Withings BIA offset where a withings-source weight/BF reading exists within ±3d of a scan ("Withings BF 31.6% = +1.7pp vs DEXA"). (b) TAPE CONTEXT: `buildTapeContext()` — always emits; last 10 `state.measLog` entries (waist/chest/larm/rarm/lthigh/rthigh/neck), deltas since first + last ~4 weeks, waist-to-height ratio (flags ≥0.5), US Navy BF estimate (male, neck+waist+height, labelled rough cross-check), STALE nudge when >21d old, empty nudge when none. (c) `seedJayTapeReminderV1` — Saturday 11:00 reminder via existing reminders system. (d) NEW `server/ask.ts`: `answerQuestion()` (Haiku, max_tokens 1000, user's coachingKey, forced `submit_answer` tool → {status green/amber/red, verdict, numbers[≤5], meaning, action}) + `buildFullHistory()` aggregates (~3k token cap: weekly weight/BF/lean/fat buckets since startDate, loss composition fat-vs-lean, protein g + g/kg vs target, per-lift first-vs-current top set + relative strength kg/kg-bw, longest holds, monthly training days, tape series, Phase 44 calibration stats). System prompt: answer only from data, real numbers/dates/kg, jargon in brackets, BIA = 7d-averages only, tape+DEXA outrank BIA, relative-strength framing, amber when unanswerable. (e) ROUTES: `POST /api/coach/ask` (requireAuth+requireOwnerCheck+aiBudget, ≤500 chars) + `GET /api/coach/context-preview` (owner, plain-text buildContext dump, no AI). buildContext + HAIKU_MODEL now exported from ai-coach.ts. (f) UI: Coach page "Ask Forge" card (owner-only) — input + 3 example chips, answer cards (status dot, bold verdict, numbers list, meaning, lime action), "Explain simpler" re-ask, last 5 Q&As session-only; More → Admin "🔍 What my coach sees" opens scrollable monospace context viewer. Verified: 10-check script (tests/_verify-context.cjs, manual) proves DEXA/tape/offset/history blocks render from dist build.

- 46: Coach report v2 + reliability + training session-lookup fix. (a) ENGINE: weekly report `MODEL` 4.7→4.8 (drop-in; benefits report + meal-plan + max-LBM calls), `MAX_TOKENS` 4000→5000. Forced tool_choice kept (reliable structured output) so adaptive thinking NOT enabled — incompatible on this SDK version; the quality lever is the prompt. (b) PROMPT: every report now opens with TWO mandatory lines before the first heading — a **Lean mass** verdict (🟢/🟡/🔴 + LBM delta, the cut's prime directive, leads every report) and a **Last week** grade of the prior report's 3 priority actions (✓/~/✗ from coach memory). New rules: BODY-COMP RECONCILIATION (DEXA > tape > Withings BIA; never report a single-week BIA lean drop as fact, cross-check waist trend + DEXA offset) and PIN THE NUMBERS (cite context figures verbatim, never recompute). (c) RELIABILITY: node-cron is in-process so a missed Sunday 09:00 tick (deploy/restart/sleep) was lost forever — this is why a Sunday report can silently not fire. Extracted the report loop into module-level `runWeeklyCoaching(minHoursSinceReport, reason)` run from FOUR triggers, each guarded by `hoursSinceLastReport` so it never double-generates: Sunday 09:00 + hourly Sunday 10:00–23:00 (threshold 24h, heals a same-day miss) + daily 12:00 + server startup (threshold 150h ≈ 6.25d, heals a fully-missed Sunday without ever firing a spurious mid-week report). A deploy now auto-recovers a missed report via the startup trigger. (d) SESSION-LOOKUP BUG FIX: `getPreviousSessionData`/`getPreviousSessions` filtered by `getSessionTypeForDate(date)` (the rigid-cycle CALENDAR type), so training off the 4-day grid (an extra rest day shifts real sessions off their scheduled dates) made progression silently MISS the last upper/lower session. New `_classifyLoggedSession(dayLog)` matches on what was actually trained — prefers `_session.sessionType` (now stored at workout start), else classifies by logged exercise IDs against the program templates. Progression math unchanged. SW cache v48.

- 47: Training session as a coach — deterministic, mostly no AI (AI only bookends: strategy brief on open, low-down on close). (a) SET-TO-SET AUTOREGULATION: `_autoregNextSet(ex,lastSet,setIdx)` reads the set just done (reps + effort tag) vs the rep range and computes the next set's weight via double-progression (topped range + easy → +inc.easy; topped → +inc.solid; below range → −inc.fail; tough-but-in-range → hold). Shown UNDER the live rest countdown (`renderWmRest`) with blunt rotating copy + colour by direction; stored on `wm.autoReg` so the next set's kg/reps pre-fill (`renderWmSet`/`renderWmTransition`). Pure arithmetic — reuses `_incForLift`/`_roundToPlate`, no AI/network. (b) PER-EXERCISE PANEL: first set shows "Last time: 100×8,8,7 · date" + a static one-line form cue from `FORM_CUES` (written once per lift, not AI). (c) SKIP: `wmSkipExercise()` marks `{skipped:true,sets:[]}` — excluded from score/volume; `_suggestWeightCore` gained a fallback so an omitted/skipped lift uses the most recent session that actually has data for it (skip ≠ deload/reset). (d) TRAINING NOTES: per-exercise running note (`state.exerciseNotes[exId]={note,addedAt,source}`, user-written via `wmAddExerciseNote`, shares the field the coach training-swap writes) + per-session note (`exLog[date]._session.note` via `wmAddSessionNote`); new atomic `PUT /api/state/exercise-notes`; `buildTrainingNotes()` block in the weekly context + per-lift NOTE line in the session brief prescriptions ("taken into consideration next session"). (e) END-OF-SESSION RECAP: `wmFinish` shows a deterministic recap — volume vs 4-week avg verdict (from Phase 44 `_session.score.pct`), effort mix, skips ("carried forward, no deload"), pre-session feel, session note; the existing Haiku reflection stays as the warm low-down topper on FINISH. (f) TAPE TODAY CARD: `renderTapeToday()` (owner) on Today like supplements/skin care — shows last-logged/days-since, amber when due (≥7d) or never, taps open `modal-meas`. Feel formula (Phase 44) unchanged. SW cache v49. NOTE: autoreg is logic-verified, not unit-tested (lives in non-modular workout.js).

- 48: Adaptive nutrition engine — measured TDEE replaces the formula guess; one carb-only change per week, governed by muscle. (a) ENGINE: new `server/nutrition.ts` `analyzeNutrition(state, asOf?)` — PURE/deterministic (no AI). Observed TDEE = avg logged intake − (14-day weight-trend energy, least-squares slope × 7700/kg); training+walking baked in automatically (the scale integrates them). Cross-checks Oura `calorieLog.total`. Confidence: high only when ≥10/14 days logged AND intake-TDEE within 12% of Oura — else LOW → no change + plain-English banner. Rate governor (muscle-first, trust order strength > tape > BIA): strength falling → ease (eat more); strength holding + waist dropping → push, capped 0.6 kg/wk; else steady 0.5. Honest floor-bound case: at the calorie floor + losing slow → hold + "add steps/cardio, don't eat less". Recommendation is carbs-only (protein/fat fixed), clamped ±150 kcal/wk, never below floor; calorie change = carbDelta×4 exactly. (b) WIRING: `generateWeeklyReport` injects an ADAPTIVE NUTRITION context block (AI narrates, pins the numbers) and appends a deterministic `nutrition-adjust` suggestion when confident+non-hold (Apply card never depends on the AI emitting it). (c) APPLY: `applyNutritionAdjust` (coaching.ts) shifts dynamicTargets rest/upper/lower calories+carbs (+ legacy calsRest/calsGym/macros), then `redistributeMealPlanCarbs` bumps the QUANTITY of carb-dominant meal-plan ingredients proportionally (scales their portion, recomputes meal totals) — never swaps/removes a food. `nutrition-adjust` added to Suggestion type union + tool enum-adjacent + SUGGESTION_TYPE_META badge ("CALORIES · CARBS", lime). (d) TESTS: `tests/_verify-nutrition.cjs` (manual, dist-based) — 7 checks: sparse→low-conf, slow+green→trim, floor-bound→hold+advice, strength-falling→add carbs, Oura-disagree→low-conf, floor respected, carbs×4=calorieDelta. User decisions baked: pace "a bit faster when muscle holds" (cap 0.6), no forced diet-break weeks, tap-to-apply only, carbs-only. SW cache v50. (Mifflin `computeTargets` stays as cold-start fallback until enough clean data.)

- 48a: "That's everything I ate today" — fixes the GLP-1 false-negative where genuinely low-intake days (Mounjaro suppresses appetite) were treated as under-logging and vetoed the adaptive engine via the Oura-disagreement check. New `state.foodComplete[date]=true` (per-day user confirmation) via dateKeyedRoute `/api/state/food-complete/:date`; Food-page toggle ("Tap when you have logged all your food today" → "✓ Everything logged today"). Engine (`analyzeNutrition`): counts `completeDays`; when ≥10 confirmed days → HIGH confidence and the Oura veto is dropped (trust real intake + weight change, since on a GLP-1 you genuinely eat below Oura's burn estimate). Report context tells the AI confirmed days are accurate even when low — "do NOT call confirmed days under-logged". 8th nutrition test: confirmed low-intake + Oura-disagree → high confidence. SW cache v51.
- 63: Rest-gap accessories done for real — fixes the duplication where a small/rehab exercise done during a compound's rest (e.g. band pull-apart between Shoulder Press sets) still showed up again at the end. The Phase 62 throwaway "filler" rest drill is superseded: the guided-mode rest screen (`renderWmRest`) now offers the SESSION'S OWN remaining accessories (any `size:'small'` or `category:'rehab'`, rep-based, not the lift being rested, not already done/skipped) with a reps stepper (+ a kg stepper for weighted ones like Lateral Raise). `wmRestLogSet(exId)` writes a REAL working set into that accessory's own `exLog[date][exId].sets` (tagged `viaRest:true`), marks it `done` at its target set count, and re-renders ONLY the panel's own rows (`#wm-rest-acc-rows`) so the countdown/next-set button are never touched. New nav helpers `_wmExComplete`/`_wmFirstUndoneSetIdx`/`_wmNextPendingIdx`/`_wmFirstPendingIdx`: `wmStartFirstSet`/`wmNextExercise`/`wmSkipExercise` now advance to the next PENDING exercise (skipping anything knocked out during rest, and resuming a part-done accessory at its next unlogged set); `renderWmExerciseDone`'s FINISH-vs-NEXT and the outline's per-exercise ✓ reflect it. Accessories logged this way count normally toward volume/score (they're real sets), so the recap needs no special-casing. Phase 62 filler data-layer functions (`FILLERS`, `getFillerForLift`, `logFillerGap`, `fillerAdherence`) remain in data.js but are no longer wired into the rest UI. Two render-smoke tests rewritten for the new flow (167 pass; the 1 pre-existing supplements-grid failure is unrelated). SW cache v91.
- 66: Real interactive charts on the Track page — two hand-drawn SVG line charts (no chart lib) replace the tiny 14-day sparklines' role as the headline visual. (a) ENGINE: new reusable chart core in `pages.js` — `_tchartHTML(cfg)` builds the SVG (grid, y/x axes, optional confidence band, target line, area-fill gradient, dashed projection, series polylines with endpoint dots + direct end-labels, and a hidden crosshair group) and registers hover data in module-level `_tchartReg`; `_initTrackCharts()` (called after `renderTrack` innerHTML, alongside `renderCompareSnapshot`) wires pointer/touch move → nearest-point crosshair + floating tooltip. Helpers: `_niceTicks(min,max,approx)` (magnitude-based nice axis steps), `_monthTicks(startDate,maxDay)` (1st-of-month x labels), `_weeklyBuckets(entries,keys,startDate)` (averages daily entries into weekly points to de-noise Withings BIA). Series colours are dataviz-validated LITERALS — Lean `#2f7fd6`, Fat `#e0680f`, Weight brand lime `#c8ff00` — deliberately NOT the app `--blue`/`--orange` (those fail CVD + lightness checks). Chart CSS lives in `index.html` (`.tchart*`). (b) WEIGHT chart (`_trackWeightChart`, in the `progress` collapsible): weekly-bucketed weight line + area fill, dashed projection from the last real point to `targetWeight` at `getProjections().wDate`, a lime confidence band that widens with distance, a dashed TARGET line, and a readout card (projected goal date + weeks-to-target + range + confidence). (c) COMPOSITION chart (`_trackCompChart`, in the `bodycomp` collapsible, retitled "Lean vs Fat"): lean vs fat mass (both from aligned weight×bf so lean+fat=weight), legend, and a "Down Xkg since <start> — Y% of it was fat" note anchored to the profile's true `startWeight`/`startBF`. (d) Both return '' (no chart, no throw) below 3 entries. Verified: `scratchpad/verify-charts.cjs` (manual) asserts SVG geometry (series spans the plot, lean sits above fat, projection/band/target present, sparse-data guard) + a Chromium screenshot of the real output matched the approved mockup. SW cache v101.
- 69: In-rest Deep Squat mobility — now a real, counting, progressing, tracker-ticking drill (replaces the Phase-63b dead `_fillers` Asian-squat filler). (a) DRILL: new standalone `MOBILITY_DRILLS` list in data.js registers `mob_deepsquat` (`metric:'time'`, `reps:'60-65s'`, `capSeconds:120`, `mobility:true`) into `getAllExercises()` — NOT in any WORKOUTS template, so it never enters a session flow or the rest-accessory rotation, but IS resolvable so `computeSessionVolume`/`getBestLift`/progression treat it as a timed exercise and count its `{seconds}` sets as seconds-volume. Excluded from the Track strength trend/records (`mobility` flag) since it isn't strength. (b) PROGRESSION: `suggestTime` gained a per-exercise `capSeconds` ceiling (via a `climb()` helper) — the deep squat climbs 60→65→…+5/session once the narrow 60-65s range is topped and stops at 120s; plank/wall-sit (no `capSeconds`) unchanged. `metric:'time'` routes it through `suggestTime`, never the weight math, so it never gets a kg suggestion. (c) REST UI: `_wmMobilityFallbackHTML`/handlers rewritten — the fallback (shown when no rehab accessory remains) is now a Deep Squat with a count-up stopwatch on its OWN interval (`wm._mobInterval`, independent of the rest countdown — cleared in `renderWmRest`/`wmStartNextSet`), Start/Done/Skip, and a target+PB line. "Done" logs a real `{seconds,done,viaRest:true}` set into `exLog[date].mob_deepsquat.sets` (`wmRestMobilityDone`), optional and never gates the next set. (d) TRACKER: new `restMobility` routine type in `STRETCH_ROUTINES` (single `rm_deep_squat` entry); "Done" also calls `markStretchDone`+`saveStretchSession(date,'restMobility')` (owner/`isStretchUser` only) so it flips `completed=true` for restMobility ONLY — never morning/evening/flexibility. `getStretchCompliance` + `renderStretchHistory` gained a 4th "Rest-Gap · X/7" tile (2×2 grid) with its own streak. Explicitly NOT done: no rep-skill progression, `getFillerForLift`/the 6-filler per-lift mapping stays dead, existing rehab-accessory logging/counting untouched. Tests: 4 new render-smoke assertions (registered+timed+seconds-volume; duration-not-weight suggestion + climb + 120 cap; restMobility-only credit + own X/7; rewritten rest-fallback flow logs a real set + ticks restMobility + no `_fillers`). 176/177 pass (the 1 supplements-grid failure is pre-existing/unrelated). SW cache v104.
- 68: Collapsible dropdown sections on the More page — mirrors the Track-page pattern (Phase 55-ish) so the long More page collapses into tappable headers. New More-scoped helpers in `pages.js` (`_MORE_DEFAULT_OPEN`, `_moreSectionState`/`_moreSectionOpen`/`toggleMoreSection`, `_mOpen(key,title,headerExtra)`/`_mClose()`) — a parallel set to the Track ones with their own localStorage bucket `forge_more_sections` and `msec-`/`msec-bd-`/`msec-chev-` id prefix, so More's open/closed state is independent of Track and the working Track code is untouched. `renderMore` wraps all 15 settings/data sections (AI Coach key, Personal Profile, Coach Settings [owner], Medications, Blood Markers, Skin Care [owner], Tell Your Coach, Food Preferences, Oura, Withings, Supplements, Training Schedule, Injuries, Profile Settings, Admin [owner]) in `_mOpen`/`_mClose`; the three owner-gated wraps sit inside the existing `${isOwner()?…}` conditionals. Action-first top stays always-visible (phase banner, water, Quick Actions); the two self-gating sub-render sections that emit their own sec-label (`renderStretchHistory`, `renderStageGuide`) left uncollapsed. All sections collapsed by default (the whole point — shorten the page); state persists per-section. Post-render init hooks unchanged — they populate fields by `getElementById`, which works on `display:none` content (grep-confirmed no layout/visibility deps). Verified: div-balance + all-15-sections-wrapped + collapsed-default + controls-survive-wrapping structural check (`/tmp`), 2 new render-smoke assertions, Track collapsible test still green (173/174 pass; the 1 supplements-grid failure is pre-existing/unrelated). SW cache v103.
- 67: Make the AI coach more personal — two changes (the coach chat already ingests BP, tape measurements, training, blood markers, meds, skin, food + ~25 streams via `buildContext`; this fills the real gaps). (a) GOAL-WIRING FIX (server): the prominent goal/phase banner (`savePhaseEdit`) writes `profile.activePhase` + top-level `profile.phase`, but `buildContext` (ai-coach.ts) only read `profile.personal.phase` — so a banner-set goal showed `CURRENT PHASE: (not specified)` and its numbers (calorieTarget/floor, targetBF range, goalWeight) never reached the coach. Now: phase resolves via `personal.phase ?? profile.phase ?? profile.activePhase.phase` (lower-cased), and a new `ACTIVE PHASE PLAN (user-set goal)` line in PLAN PROFILE surfaces the banner's calorie target/floor, protein floor, goal weight, target-BF range, start weight/date. `PUT /api/state/profile/active-phase` (state.ts) now also mirrors phase into `profile.personal.phase` (extra jsonb_set layers) and `savePhaseEdit` (app.js) sets it optimistically — so all three phase locations stay consistent going forward. No migration (the read fallback covers existing data). (b) "ABOUT ME" FREE-TEXT: new `profile.aboutMe = {text, updatedAt}` (≤2000 chars) via new atomic `PUT /api/state/profile/about-me` (jsonb_set, mirrors food-prefs). `buildContext` prepends an `ABOUT THIS PERSON (in their own words — weight this heavily)` block at the very top (before DEMOGRAPHICS) so both the chat and the weekly report treat it as governing context. New More-page "Tell Your Coach About You" section (`#am-text` textarea + char counter, `saveAboutMe()` via offline-safe `saveFieldToServer`). Not owner-gated. Verified: `/tmp` context spot-check (banner-set phase w/ personal.phase absent → resolves + goal numbers + About-me at top) + 2 new render-smoke assertions (172/173 pass; the 1 supplements-grid failure is pre-existing/unrelated). SW cache v102.
- 65a: Chat prompt caching (server-only). `chatAnswer` (server/ask.ts) now sends its `system` field as a single text block tagged `cache_control:{type:"ephemeral"}` instead of a bare string. The system prefix (CHAT_SYSTEM + `buildContext` + `buildFullHistory`) is byte-identical across turns of a thread, so follow-up turns within the 5-min TTL read it at ~0.1x instead of reprocessing the whole context — a real cost/latency win on multi-turn chats. Message history stays after the breakpoint (the volatile part). Haiku 4.5's minimum cacheable prefix is 4096 tokens; below that it's a silent no-op (no error, just `cache_creation_input_tokens:0`), above it's a cache hit — the owner's rich context clears it comfortably. Only the chat is cached: the weekly report is a single Opus call (no repeated prefix) and Ask Forge puts context in the user turn (single-shot), so neither benefits. Chat stays owner-only (BYOK requires the user's own Anthropic key; family users don't have one — opening the gate would just surface "no API key" errors). tsc clean; no frontend/API-shape change.
- 65: Conversational AI coach (owner-only). The rich context (`buildContext` + `buildFullHistory`) previously fed only the weekly report + a one-shot "Ask Forge"; now it powers a threaded, advisory chat with memory. New `chatAnswer(userId, messages)` in `server/ask.ts` (Haiku, plain-text, multi-turn; the big identical-per-turn context goes in `system`, message history passed through — future prompt-cache candidate). New route `POST /api/coach/chat` (`coach-settings.ts`, `requireAuth + requireOwnerCheck + aiBudget`, 500 chars/msg, 24 msgs/req, last must be user). Thread persisted in `state.coachChat` via new `PUT /api/state/coach-chat` (own-key `jsonb_set`, capped 40, race-safe) so it survives reloads (Ask Forge was session-only). Frontend: the Coach page "Ask Forge" card became "Chat with Forge" — a scrollable bubble thread (`_coachChatThreadHTML`) rendered from `STATE.coachChat`, `sendCoachChat()` in app.js (optimistic user turn + "Thinking…", posts the last ~12 turns, appends the reply via `formatCoachingReport`, persists through `saveFieldToServer` for offline-queue/retry; reverts the optimistic turn on error to keep the thread alternating), example-question chips + Clear chat. v1 is ADVISORY-only (no Apply-in-chat — those stay in the weekly report), owner-gated, client-persisted; the old `askForge`/`/api/coach/ask` remain but are no longer wired to UI. SW cache v100.
- 64: Data-loss race fix (server-only). Several paths did whole-`state` read-modify-write (`findUnique` → mutate JS → `update({data:{state}})`), silently clobbering any concurrent write to a *different* key — worst on the HOURLY Oura/Withings syncs, which rewrote the entire state while the user was logging. New `server/state-merge.ts` with 3 race-safe writers: `mergeStateMap(userId,key,partialMap)` (shallow `jsonb ||` merge of only the changed date-entries into the LIVE column — sibling dates/keys preserved), `setStateKey(userId,key,value)` (single-key `jsonb_set` whole-object/scalar write), and `casUpdateState(userId,mutateFn)` (compare-and-swap on `updatedAt` TIMESTAMP(3), retries on a lost race; `mutate` returns null to skip) for the genuinely multi-key / same-array paths. Converted: `oura.ts` (per-key partial merges honouring the manual-source guard; token re-encrypt + ouraLastSync via own keys), `withings.ts` (token refresh → `setStateKey('withings')`; weightLog/bfLog/bodyComp/lastSync → one CAS, manual-wins intact), `ai-coach.ts` saveReport (CAS array-prepend) + saveMealPlan/recompute (`setStateKey`), notifications (cron GLP-1/recap adds + `state.ts` mark-read/expire → CAS, both halves together), coaching apply/dismiss → CAS (validation + mutation inside the swap for idempotency; apply response now strips `SERVER_ONLY_FIELDS` before echoing state — closes a secret-leak). The whole `PUT /api/state` (offline-queue fallback) is intentionally left as-is. Verified against a REAL Postgres: `tests/_verify-state-merge.cjs` (manual, dist-based, DB-guarded) — 8 checks incl. two concurrency races (parallel CAS to different keys; a sync + a notification-read at once) that a plain RMW would lose; all pass.
- 63a: Tuned the rest-accessory panel defaults in `_wmAccessoryRowsHTML` — instead of a static rep-range top + progression-only weight, the reps stepper now defaults to what you actually did last session for that set position (via `getLastExercisePerformance`, referencing the same set index, else the last set), nudging you to match/beat it; falls back to the rep-range top for a first-timer. Weight defaults to the progression suggestion, then last-logged kg, then empty. Each row shows a compact `last 8kg×14` reference. New render-smoke test asserts the history-grounded defaults. SW cache v92.

## Skipped/deferred
- Photos to R2 (entire feature removed in Phase 12)
- Workout customisation editor (deferred, may revisit)
- Phase 15 (UI polish, de-hardcoding) — explicitly skipped per user

## Known issues / open work
- ~~Calorie Stage Guide hardcoded~~ FIXED Phase 42e (computed from profile)
- ~~Eating window hardcoded~~ FIXED Phase 42e (profile.eatingWindow; wizard default off)
- ~~No password reset flow~~ FIXED Phase 42f (owner-run admin reset; no self-service email flow)
- prompt()/confirm() modals throughout (Edit Targets, Reset, admin password reset)
- No accessibility (ARIA, keyboard nav)
- No real charts (sparklines only)
- No exercise customisation (Phase 42d added 3 program templates; per-exercise editing still deferred)
- New-user onboarding anchors trainingStartDate to signup date (correct for family; Jay keeps 2026-05-08)
- ~~Race condition: background/AI-save paths did whole-state RMW~~ FIXED Phase 64 (Oura/Withings sync, ai-coach saves, notifications, coaching apply/dismiss now use `state-merge.ts` per-key/CAS writes). Full state PUT is still the offline-queue fallback for non-hot fields (foods/exLog/water/weight/sleep use field-scoped endpoints); wizard completion uses full PUT (fresh state, single device — acceptable)
- "Reset All Data" now clears Oura/Withings tokens (Phase 40); reminders + coaching key still survive
- Server-side AI coach (ai-coach.ts) still describes the upper/lower program in some context blocks — fine for Jay; family users on full-body/home programs will get slightly mismatched training framing until coach context reads profile.programId
- No CI yet — `npm test` exists (25 tests) but isn't enforced on push

## How coaching works (Phase 23 — BYOK)
1. Each user pastes their own Anthropic API key on the More page; Forge encrypts it with AES-256-GCM (`ENCRYPTION_KEY`) and stores in `state.coachingKey`.
2. Sunday 09:00 UK cron runs `generateWeeklyReport(userId)` for every user with a key. Skips if a report exists in the last 24h.
3. Report is generated by Opus 4.7 via tool use (forced structured JSON output: `{title, content, suggestions[]}`).
4. Suggestions appear on the Coach page with Apply/Dismiss buttons. Apply types: `macros` (writes profile.calsGym/calsRest/macros), `reminders` (adds/removes), `note` (informational).
5. Manual "Generate Report Now" button on More page (rate-limited to 1/hour).

- 35: Skin Care tracker (owner-only) — `state.skinCare = {products:[]}` + `state.skinCareLog[date]`. Products carry type/concentration/slot(am/pm/both)/frequency/startedDate. New `isOwner()` gate (profile.email/name). Field-scoped `PUT /api/state/skin-care` + `/skin-care-log/:date`. More page "Skin Care Routine" section (owner-only). Today page AM/PM checklist showing only products due that day per `skinDueOn()`; one-tap post-retinol irritation toggle (😊/😬) as the tolerance signal. `modal-skin-edit` for products.
- 36: AI weekly skin review — skin care folds into the Sunday Opus report. buildContext gains a SKIN CARE ROUTINE block (products, weeks-on-frequency, 14d adherence + irritation flag counts). System prompt gains retinol-ramping rules (hold 2-4wk per frequency, step up only with zero irritation flags, ladder weekly→every-4→3→2→daily), routine-structure rules (vit C AM, retinol PM, SPF AM, no clashing actives), and honest loose-skin guidance tied to weight-loss rate + age. New `skincare` suggestion type — payload `{productId, frequency}` — Apply handler in coaching.ts updates the product frequency + sets `frequencyStartedAt`.

- 37: Skin care complete overhaul (owner-only, jay@afjltd.co.uk). (1) `seedJaySkinCareV1()` migration replaces `state.skinCare` with Jay's real 9-product routine + `phase`/`phaseStartDate`/`tretinoinReady`/`weeklyCheckIn`. (2) `requireOwnerCheck` middleware 403s non-owners on all 4 skin endpoints; 2 new endpoints `PUT /api/state/skin-care-weekly/:date` (Sunday journal) + `/skin-care-phase` (retinol phase advance, re-frequencies retinol+cicaplast via phase→freq map). (3) `getSkinVisibleItems()` conflict engine — single source of truth: AM fixed order, PM branches on retinol night (hides arbutin+niacinamide on retinol nights, hides cicaplast otherwise, serums thinnest-first, honey mask last). Today checklist shows compliance score, Mounjaro-Wednesday banner, retinol-night banner, CE-Ferulic→SPF reminder, 5-level irritation selector (none/mild-dryness/peeling/redness/burning). (4) Retinol Journey card — 6-phase roadmap, Advance button gated on 3 conditions (3wk min, no redness/burning 14d, 100% retinol compliance 14d), tretinoin milestone at phase 5. (5) Sunday weekly journal modal (1-10 score, trend, notes). (6) `buildSkinContext()` feeds the weekly Opus report: products, 14d compliance (overall/retinol/AM-days/PM-days/streak), 14d irritation counts + burning flag + Mounjaro-Thursday correlation, last-4 journal entries, phase readiness. System prompt has retinol-phase rules + routine-structure rules + Jay's skin-health context. New `skincare-phase` suggestion type. (7) More page: per-product 30d compliance %, 7-day tick history squares, retinol phase badge, read-only Routine Rules card. `5x-week` frequency added for phase 4 (weekday-based). `state.skinCareLog[date]` carries `_compliance` + `_mounjaro_day` + `_skin_score/_skin_trend/_skin_notes`.

## Cowork removed (Phase 34)
- The entire legacy Cowork PAT integration is gone — UI section, frontend token functions, and all 5 dead backend route files (tokens/export/mealplan/profile-routes/reminders-routes), the `requireAccessToken` middleware, and the legacy `POST/DELETE /api/coaching-reports` routes. The `AccessToken` Prisma model + its migrations are left in place (dormant, unused table — dropping it would need a migration for no real gain).
- All coaching is now BYOK only (see "How coaching works" above).

## Field-scoped state endpoints (used by frontend)
- PUT /api/state/foods/:date, /api/state/exLog/:date, /api/state/water/:date
- PUT /api/state/weight, /api/state/sleep/:date
- PUT /api/state/meal-plan, /api/state/profile/{personal,medications,food-prefs,blood-markers}
- Meal plan format: meals[] with structured ingredients[] and supplements[] arrays

## Style conventions
- Lime (#c8ff00) primary, black (#080808) bg, Archivo Black for headlines
- Mobile-first, max-width 480px on desktop
- No external chart libs except Chart.js (CDN, only when needed)
- TypeScript strict on backend, vanilla JS on frontend (no build for /public/)
